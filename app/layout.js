export const metadata = {
  title: 'AI Demo Landing Pages',
  description: 'A showcase of demo landing pages across different industries.',
}

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
